apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: home-assistant
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: home-assistant
  filterTags:
    pattern: '^2023\.8\.(?P<ts>[0-9]+)$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
